apiVersion: v1
kind: Service
metadata:
  labels:
    app: mailhog
  name: mailhog
spec:
  type: ClusterIP
  ports:
    - port: 1025
      protocol: TCP
      targetPort: 1025
    - port: 8025
      protocol: TCP
      targetPort: 8025
  selector:
    app: mailhog
