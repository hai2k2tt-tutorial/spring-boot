apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana-route
spec:
  parentRefs:
    - name: api-gateway
  hostnames:
    - grafana.local
  rules:
    - backendRefs:
        - name: grafana
          port: 3000
