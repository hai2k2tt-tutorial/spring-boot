apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prometheus-route
spec:
  parentRefs:
    - name: api-gateway
  hostnames:
    - prometheus.local
  rules:
    - backendRefs:
        - name: prometheus
          port: 9090
