apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: loki-route
spec:
  parentRefs:
    - name: api-gateway
  hostnames:
    - loki.local
  rules:
    - backendRefs:
        - name: loki
          port: 3100
