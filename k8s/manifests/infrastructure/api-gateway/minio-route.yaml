apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: minio-route
spec:
  parentRefs:
    - name: api-gateway
  hostnames:
    - minio.local
  rules:
    - backendRefs:
        - name: minio
          port: 9000
