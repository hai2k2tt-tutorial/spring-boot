apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: shop-wallet-fe-route
spec:
  parentRefs:
    - name: api-gateway
  hostnames:
    - shop-wallet-fe.local
  rules:
    - backendRefs:
        - name: shop-wallet-fe
          port: 3007
