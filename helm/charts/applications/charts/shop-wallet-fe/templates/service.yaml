{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: shop-wallet-fe
  namespace: {{ .Values.namespace }}
  labels:
    app: shop-wallet-fe
spec:
  type: ClusterIP
  selector:
    app: shop-wallet-fe
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
{{- end }}
