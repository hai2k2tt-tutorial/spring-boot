{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: shop-service
  namespace: {{ .Values.namespace }}
  labels:
    app: shop-service
spec:
  type: ClusterIP
  selector:
    app: shop-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
{{- end }}
