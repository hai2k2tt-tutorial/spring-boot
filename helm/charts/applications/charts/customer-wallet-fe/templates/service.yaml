{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: customer-wallet-fe
  namespace: {{ .Values.namespace }}
  labels:
    app: customer-wallet-fe
spec:
  type: ClusterIP
  selector:
    app: customer-wallet-fe
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
{{- end }}
