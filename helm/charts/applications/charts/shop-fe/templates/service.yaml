{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: shop-fe
  namespace: {{ .Values.namespace }}
  labels:
    app: shop-fe
spec:
  type: ClusterIP
  selector:
    app: shop-fe
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
{{- end }}
