{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: wallet-service
  namespace: {{ .Values.namespace }}
  labels:
    app: wallet-service
spec:
  type: ClusterIP
  selector:
    app: wallet-service
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
{{- end }}
