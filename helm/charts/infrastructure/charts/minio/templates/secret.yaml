{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: minio-secrets
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  root_user: {{ .Values.rootUser | b64enc }}
  root_password: {{ .Values.rootPassword | b64enc }}
{{- end }}
