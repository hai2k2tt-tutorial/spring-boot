{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: minio-config
  namespace: {{ .Values.namespace }}
data:
  bucket_product_images: {{ .Values.bucket.productImages | quote }}
{{- end }}
