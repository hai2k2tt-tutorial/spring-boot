apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tempo-pvc
  namespace: {{ .Values.namespace }}
spec:
  storageClassName: 'standard'
  volumeName: tempo-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
