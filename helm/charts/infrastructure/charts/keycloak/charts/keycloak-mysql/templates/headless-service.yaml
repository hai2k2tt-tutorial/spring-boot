apiVersion: v1
kind: Service
metadata:
  name: keycloak-mysql-headless
  namespace: {{ .Values.namespace }}
  labels:
    app: keycloak-mysql
spec:
  clusterIP: None
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  selector:
    app: keycloak-mysql
