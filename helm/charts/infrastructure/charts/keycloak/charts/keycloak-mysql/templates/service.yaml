apiVersion: v1
kind: Service
metadata:
  name: keycloak-mysql
  namespace: {{ .Values.namespace }}
  labels:
    app: keycloak-mysql
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  selector:
    app: keycloak-mysql

